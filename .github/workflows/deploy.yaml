name: Maven Property Evaulator Test
on:
  repository_dispatch:
    types: [test-event]
  create:
  push:
    branches: [main]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: joshlong/maven-property-evaluator@v1
      - name: Test
        run: |
          # export PATH=$GITHUB_WORKSPACE/bin/:$PATH
          echo "MAVEN EXPRESSION: ${MAVEN_EXPRESSION}"
